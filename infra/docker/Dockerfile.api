FROM python:3.12-slim

WORKDIR /app
COPY . /app
RUN pip install --upgrade pip && pip install -e ".[api,tracking,streaming,model]"

ENV PYTHONPATH=/app/services/fraud_platform/src
CMD ["python", "-m", "fraud_platform.api.server"]

