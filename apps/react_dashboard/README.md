# React Recruiter Dashboard

This app is the richer frontend surface for the fraud platform.

## Run

```bash
npm install
npm run dev
```

Set `VITE_API_BASE=http://localhost:8000` if your API is not running on the default port.
